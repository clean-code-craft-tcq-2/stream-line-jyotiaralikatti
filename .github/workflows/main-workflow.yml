
Pull requests
Issues
Marketplace
Explore
@jyotiaralikatti
clean-code-craft-tcq-2 /
tdd-buckets-jyotiaralikatti
Public
generated from clean-code-craft-tcq-1/test-driven-ranges

Code
Issues
Pull requests 1
Actions
Projects
Wiki
Security
Insights

    Settings

tdd-buckets-jyotiaralikatti/.github/workflows/main-workflow.yml
@jyotiaralikatti
jyotiaralikatti Create main-workflow.yml
Latest commit b1371e7 28 days ago
History
1 contributor
32 lines (23 sloc) 691 Bytes
name: Build and Run

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: install coverage tool
      run: |
        sudo apt-get update
        sudo apt-get install -y gcovr
    - name: build
      run: g++ -Wall -Wextra -fprofile-arcs -ftest-coverage -o testchecker *.c*
      
    - name: run
      run: ./testchecker

    - name: collect coverage
      run: gcovr --gcov-exclude=".*test.*" -r . --html --html-details -o coverage.html --print-summary

    - name: 'Upload Artifact'
      uses: actions/upload-artifact@v2
      with:
        name: coverage
        path: ./*.html
        retention-days: 21
